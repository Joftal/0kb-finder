<Window x:Class="acomp.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:acomp"
        mc:Ignorable="d"
        Title="0KB文件扫描器" Height="700" Width="900"
        WindowStartupLocation="CenterScreen"
        Background="#F5F5F5"
        FontFamily="Segoe UI">
    <Window.Resources>
        <local:BoolToTextConverter x:Key="BoolToTextConverter"/>
        <SolidColorBrush x:Key="AccentBrush" Color="#007ACC"/>
        <SolidColorBrush x:Key="SecondaryBrush" Color="#E1E1E1"/>
        <SolidColorBrush x:Key="CardBackground" Color="White"/>
        <Style TargetType="Button">
            <Setter Property="Margin" Value="5"/>
            <Setter Property="Padding" Value="15,8"/>
            <Setter Property="Background" Value="{StaticResource AccentBrush}"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4" 
                                Padding="{TemplateBinding Padding}">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#005A9E"/>
                            </Trigger>
                            <Trigger Property="IsPressed" Value="True">
                                <Setter Property="Background" Value="#004080"/>
                            </Trigger>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#CCCCCC"/>
                                <Setter Property="Foreground" Value="#666666"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
        <Style TargetType="ListBox">
            <Setter Property="Background" Value="{StaticResource CardBackground}"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Padding" Value="5"/>
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="TextBlock">
            <Setter Property="FontSize" Value="14"/>
        </Style>
        <Style TargetType="Expander">
            <Setter Property="Background" Value="#F0F0F0"/>
            <Setter Property="BorderBrush" Value="{StaticResource SecondaryBrush}"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Margin" Value="0,10,0,5"/>
            <Setter Property="Padding" Value="10"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Expander">
                        <Border Background="{TemplateBinding Background}" 
                                BorderBrush="{TemplateBinding BorderBrush}" 
                                BorderThickness="{TemplateBinding BorderThickness}" 
                                CornerRadius="4">
                            <Grid>
                                <Grid.RowDefinitions>
                                    <RowDefinition Height="Auto"/>
                                    <RowDefinition Height="Auto"/>
                                </Grid.RowDefinitions>
                                <Grid Grid.Row="0">
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <ContentPresenter ContentSource="Header" Grid.Column="0" VerticalAlignment="Center"/>
                                    <ToggleButton Grid.Column="1" IsChecked="{Binding IsExpanded, RelativeSource={RelativeSource TemplatedParent}}" 
                                                  Background="Transparent" BorderThickness="0" Margin="5,0">
                                        <ToggleButton.Content>
                                            <TextBlock Text="{Binding IsChecked, RelativeSource={RelativeSource AncestorType=ToggleButton}, Converter={StaticResource BoolToTextConverter}}" FontSize="12"/>
                                        </ToggleButton.Content>
                                    </ToggleButton>
                                </Grid>
                                <ContentPresenter Grid.Row="1" x:Name="ExpandSite" ContentSource="Content" Visibility="Collapsed"/>
                            </Grid>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsExpanded" Value="True">
                                <Setter TargetName="ExpandSite" Property="Visibility" Value="Visible"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>
    </Window.Resources>
    <Grid Background="{StaticResource CardBackground}" Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Selection Panel -->
        <Border x:Name="SelectionPanel" Grid.Row="0" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource SecondaryBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,20" Padding="20">
            <StackPanel>
                <TextBlock Text="📁 选择文件夹" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,10"/>
                <TextBlock Text="拖拽文件夹到下方列表，或点击添加按钮：" FontSize="12" Foreground="Gray" Margin="0,0,0,10"/>
                <ListBox x:Name="FoldersListBox" Height="400" AllowDrop="True" Drop="FoldersListBox_Drop" Margin="0,0,0,15"/>
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Center">
                    <Button Content="➕ 添加文件夹" Click="AddFolderButton_Click"/>
                    <Button Content="🔍 开始扫描" Click="ScanButton_Click"/>
                </StackPanel>
            </StackPanel>
        </Border>

        <!-- Results Panel -->
        <Border x:Name="ResultsBorder" Grid.Row="1" Background="{StaticResource CardBackground}" BorderBrush="{StaticResource SecondaryBrush}" BorderThickness="1" CornerRadius="8" Margin="0,0,0,20" Padding="20" Visibility="Collapsed">
            <ScrollViewer x:Name="ResultsScrollViewer" VerticalScrollBarVisibility="Auto">
                <StackPanel>
                    <TextBlock x:Name="ResultsTitle" Text="🔍 扫描结果" FontSize="18" FontWeight="SemiBold" Margin="0,0,0,15"/>
                    <ItemsControl x:Name="FilesItemsControl">
                        <ItemsControl.ItemTemplate>
                            <DataTemplate>
                                <Expander IsExpanded="True" Margin="0,0,0,10">
                                    <Expander.Header>
                                        <TextBlock Text="{Binding Name}" FontWeight="SemiBold" FontSize="16" Foreground="#333"/>
                                    </Expander.Header>
                                    <ItemsControl ItemsSource="{Binding Items}">
                                        <ItemsControl.ItemTemplate>
                                            <DataTemplate>
                                                <Border BorderBrush="{StaticResource SecondaryBrush}" BorderThickness="1" Margin="0,2" Padding="10" CornerRadius="4" Background="White">
                                                    <Border.Style>
                                                        <Style TargetType="Border">
                                                            <Style.Triggers>
                                                                <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                                                    <Setter Property="Opacity" Value="0.5"/>
                                                                </DataTrigger>
                                                            </Style.Triggers>
                                                        </Style>
                                                    </Border.Style>
                                                    <Grid>
                                                        <Grid.ColumnDefinitions>
                                                            <ColumnDefinition Width="*"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                            <ColumnDefinition Width="Auto"/>
                                                        </Grid.ColumnDefinitions>
                                                        <TextBlock Text="{Binding Path}" Grid.Column="0" VerticalAlignment="Center" FontSize="13">
                                                            <TextBlock.Style>
                                                                <Style TargetType="TextBlock">
                                                                    <Style.Triggers>
                                                                        <DataTrigger Binding="{Binding IsDeleted}" Value="True">
                                                                            <Setter Property="Foreground" Value="Gray"/>
                                                                            <Setter Property="TextDecorations" Value="Strikethrough"/>
                                                                        </DataTrigger>
                                                                    </Style.Triggers>
                                                                </Style>
                                                            </TextBlock.Style>
                                                        </TextBlock>
                                                        <Button Content="📂 打开文件夹" Grid.Column="1" Click="OpenFolderButton_Click" Tag="{Binding}" FontSize="12"/>
                                                        <Button Content="🗑️ 删除" Grid.Column="2" Click="DeleteButton_Click" Tag="{Binding}" FontSize="12"/>
                                                    </Grid>
                                                </Border>
                                            </DataTemplate>
                                        </ItemsControl.ItemTemplate>
                                    </ItemsControl>
                                </Expander>
                            </DataTemplate>
                        </ItemsControl.ItemTemplate>
                    </ItemsControl>
                </StackPanel>
            </ScrollViewer>
        </Border>

        <!-- Rescan Button -->
        <StackPanel Grid.Row="2" HorizontalAlignment="Center">
            <Button x:Name="RescanButton" Content="🔄 重新扫描" Click="RescanButton_Click" Visibility="Collapsed"/>
        </StackPanel>
    </Grid>
</Window>
